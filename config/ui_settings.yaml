#mode: "ANNOTATION"
mode: "ALL"
retrieval:
  top_k: 5
  similarity_threshold: 0.7
  diversity: true

generation:
  model: mistral-7b-instruct
  temperature: 0.3
  max_length: 500

ui:
  theme: Ocean Blue
  results_per_page: 10
  show_timestamps: true

advanced:
  cache_enabled: true
  cache_size: 100
  log_level: INFO